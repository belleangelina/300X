# 300X V2 论坛能力台账

> 更新日期：2026-08-14。本台账只记录脱敏路径与验证状态，不保存账号、Cookie、`formhash`、完整 HTML、帖子正文或私信内容。当前仍处于开发中，不能据此宣称 V2 或移动端完成。Linux Debug 实机从本节点起优先用按 Key 点击（`docs/engineering/linux-debug-ui.md`），不再用 xdotool 点业务控件。

状态口径：

- `代码已接入`：已有可达实现路径，不等于真实论坛或目标平台验收。
- `自动化通过`：仅指本地 fixture、单元、Widget 或假服务测试覆盖的对应契约。
- `只读 GET/契约`：使用隔离会话核对过真实移动网页的只读页面、入口或标准表单；未执行论坛业务 POST。
- `Linux 实际`：只认 `.artifacts/validation/v2-stage-a-linux/REPORT.md`、`.artifacts/validation/v2-linux-readonly-2026-08-14/REPORT.md` 与 `.artifacts/validation/v2-debug-ui-linux/REPORT.md` 中有明确路径和截图的项目；构建、自动化或命令行只读探测不计入。
- Android 与 iOS 均须分别完成实机路径，当前全部未验。

## 读取、壳层与本地状态

| 能力 | 当前实现 | 自动化 | 真实站 | Linux 实际 | Android | iOS |
| --- | --- | --- | --- | --- | --- | --- |
| 启动进入主界面、四主入口、进入 App 后登录提示 | 代码已接入；横屏列表与右栏详情均按入口独立保活 | 首次初始化、重复点击刷新、主入口切换后详情不重载通过 | 不适用 | 已验：启动、四入口、未登录论坛提示；未人工验详情状态保留 | 未验 | 未验 |
| 收藏“漫画 / 小说 / 全部”壳层及首页统一风格 | 代码已接入 | 通过 | 只读 GET/契约 | 已验：三页签、漫画/小说在线列表、全部分类下拉；群组分类当前账号为空 | 未验 | 未验 |
| 论坛首页、分区、版块 | 原生代码已接入 | 通过 | 只读 GET/契约 | 已验：论坛首页和版块入口 | 未验 | 未验 |
| 版块初始主题列表、筛选栏和公告列表 | 原生代码已接入 | 通过 | 只读 GET/契约 | 已验：初始页面显示；未操作筛选或分页 | 未验 | 未验 |
| 版块动态分类、最新/热门/精华筛选和分页 | 原生代码已接入 | 通过 | 只读 GET/契约 | 部分已验：管理版热门与精华（精华 1/1 页）；多页下一页未点到 | 未验 | 未验 |
| 公告详情 | 原生代码已接入；版块入口 URI 归一为 `mod/id/mobile=2`；正文下载走认证附件管线，外链仍系统打开 | 内链主题、外链系统打开、公告附件 Referer/下载、版块多参数公告 URI 归一通过 | 入口已盘点 | 已验：欢迎光临公告正文；附件未点 | 未验 | 未验 |
| 主题初始页、楼层正文和返回版块 | 原生代码已接入 | 通过 | 只读 GET/契约 | 已验：单个主题初始页及返回；未验分页和阅读顺序切换 | 未验 | 未验 |
| 主题分页、阅读顺序和全部可见楼层 | 原生代码已接入；同 tid 全站置顶允许从其他版块打开并采用页面真实 fid | 同 tid 跨版 fid 采用页面 fid 通过 | 只读 GET/契约 | 部分已验：倒序/只看楼主芯片切换；分页未操作 | 未验 | 未验 |
| `findpost` 精确 pid 定位 | 原生代码已接入 | 通过 | 入口已盘点 | 未验 | 未验 | 未验 |
| 主题内点评与评分摘要 | 原生结构化只读；完整评分页原生解析关闭。点评/评分写入入口见写操作表，仅 Android/iOS 受控原页 | 通过 | 脱敏只读响应契约已核对 | 部分已验：置顶主题评分摘要可见；点评区未单独点开 | 未验 | 未验 |
| 帖内图片、链接和附件展示 | 原生代码已接入；受保护附件走当前账号会话、严格同源下载和系统打开，失败不回退浏览器 | 仓库/控制器/打开桥/主题页与缓存清理回归通过 | 只读 GET/契约 | 未验 | 未验 | 未验 |
| 全站论坛搜索 | 原生代码已接入 | 通过 | 只读 GET/契约 | 已验：入口及一次普通关键字结果 | 未验 | 未验 |
| 当前版块搜索 | 原生代码已接入；认 `body#search.pg_curforum` 与 `search.php?mod=curforum` action，并校验 srhfid | 表单/仓库覆盖 pg_curforum 与错 fid action | 入口及表单已盘点 | 已验：本版搜索页显示当前版块范围；按 Key 打开并输入探测词后未提交 | 未验 | 未验 |
| 原始收藏“全部”及动态分类 | 原生代码已接入；严格识别的用户空间收藏进入原生资料页，未知类型保持禁用 | 动态分类、异构列表、用户空间 URI/uid 绑定和路由通过 | 只读 GET/契约 | 部分已验：“全部”和分类选择器；未选择分类结果、用户空间或分页 | 未验 | 未验 |
| 群组版块与群组分类收藏 | 原生连续列表、服务端分页及版块路由已接入；映射和列表缓存按 uid 隔离 | 通过 | 群组入口、最终同 fid 版块及分类列表结构已用脱敏 GET 核对 | 未验 | 未验 | 未验 |
| 日志收藏正文、评论及站内路由 | 原生结构化只读；正文与评论不落盘、无写入口 | 通过 | 移动正文与真实评论节点结构已用脱敏 GET 核对 | 未验 | 未验 | 未验 |
| 相册收藏元数据与图片 | 原生只读；正文不落盘，外域媒体不携 Cookie 或论坛 Referer | 通过 | 移动相册及图片节点结构已用脱敏 GET 核对 | 未验 | 未验 | 未验 |
| 账号 Cookie、论坛/收藏缓存和草稿隔离 | 代码已接入 | 通过 | 不适用 | 未验 | 未验 | 未验 |
| 论坛与作品阅读模式双向跳转 | 原生代码已接入 | 通过 | 结构契约已核对 | 部分已验：阅读器「讨论」进入对应主题；主题栏阅读模式误开回复页未提交 | 未验 | 未验 |
| “我的”登录状态与 V1 二级入口 | 代码已接入 | 通过 | 不适用 | 已验：入口呈现；未逐项进入二级页面 | 未验 | 未验 |

## 社区读取与摘要同步

| 能力 | 当前实现 | 自动化 | 真实站 | Linux 实际 | Android | iOS |
| --- | --- | --- | --- | --- | --- | --- |
| 论坛首页通知、私信和个人资料动态入口 | 原生代码已接入 | 通过 | 入口已盘点 | 部分已验：社区菜单可见私信/个人资料；未打开通知或私信以免服务端已读 | 未验 | 未验 |
| 通知分类、未读、列表及 tid/pid/uid 跳转 | 原生只读代码已接入 | 通过 | 只读 GET/契约；读取列表可能触发服务端已读 | 未验 | 未验 | 未验 |
| 私信会话列表、分页及会话正文 | 原生只读代码已接入 | 通过 | 只读 GET/契约；读取会话可能触发服务端已读 | 未验 | 未验 | 未验 |
| 当前用户及其他用户的个人资料 | 原生只读代码已接入；搜索、版块、主题作者进入资料页；本人资料「收藏」打开已有原生全部收藏，他人收藏保持禁用 | 搜索/版块/主题作者、本人收藏入口与他人禁用通过 | 只读 GET/契约 | 部分已验：本人资料页打开，日志/签到禁用；本人「收藏」入口已按 Key 点中但未进入列表；他人资料未点开 | 未验 | 未验 |
| 本人主题、回复、分页及 tid/pid 跳转 | 原生只读代码已接入 | 通过 | 只读 GET/契约 | 部分已验：本人主题列表及主题/回复页签；分页未操作 | 未验 | 未验 |
| 好友、在线成员、访客和访问记录 | 原生只读代码已接入 | 通过 | 只读 GET/契约 | 部分已验：好友页签为空列表；访客列表有条目；在线/足迹页签未点开；未进私信会话 | 未验 | 未验 |
| 登录完成、回到前台、返回首页后的论坛摘要刷新 | 代码已接入；只刷新已初始化且已登录的论坛入口 | 通过 | 未做 App 路径 | 未验 | 未验 | 未验 |

社区读取代码、自动化和真实站只读契约均不等于 Linux App 实际闭环。2026-08-14 的 Linux 报告进入了社区菜单、本人资料、本人主题、好友空列表和访客列表；通知/私信正文仍未打开。

## 写操作

| 能力 | 当前实现 | 自动化 | 真实站 | Linux 实际 | Android | iOS |
| --- | --- | --- | --- | --- | --- | --- |
| 发主题（不含附件） | 原生动态表单、确认、单次提交与读回代码已接入 | 标准表单契约通过 | GET 表单已核对；未执行 POST | 未验 | 未验 | 未验 |
| 主题收藏 | 原生标准表单及作品聚合收藏路径已接入 | 标准表单与防双击通过 | GET 表单已核对；未执行 POST | 未验 | 未验 | 未验 |
| 取消主题收藏 | 原生标准表单及收藏页取消路径已接入 | 标准表单与防双击通过 | GET 表单已核对；未执行 POST | 未验 | 未验 | 未验 |
| 站内分享 | 原生动态表单、确认、单次提交与读回代码已接入 | 标准表单契约通过 | GET 表单已核对；未执行 POST | 未验 | 未验 | 未验 |
| 版块收藏 | 原生添加/取消入口、确认、完整列表回读和防重封存；添加是 GET 即突变，取消是 GET 确认页后 POST | fixture/mock 覆盖入口/最终页、opaque `handlekey`、uid 租约、多页回读、确认表单、并发/重启防重、缓存禁写与人工解除 | 已确认添加 GET 会直接写入；探针意外新增 2 条；取消只读核对确认页，未执行取消 POST | 未验 | 未验 | 未验 |
| 回复、引用（不含附件） | 原生动态表单、草稿、确认、单次提交、结果未知回读及主题/楼层入口已接入 | 真实形状合同、目标绑定、普通回复按 tid/引用按 pid 的持久防重和 Widget 路径通过 | reply/quote GET 标准表单已核对；未执行 POST，成功响应未知 | 未验 | 未验 | 未验 |
| 编辑（不含附件） | 原生动态表单、草稿、确认、单次提交、楼层回读及按 pid 持久防重已接入 | 真实 GET 表单形状、严格目标绑定、unknown 与 Widget 路径通过 | 编辑 GET 标准表单已核对；未执行 POST，成功响应未知 | 未验 | 未验 | 未验 |
| 删除 | 明确不适用：未注册原生 Action，当前账号样本没有可见入口或标准表单，UI 无删除按钮 | fail closed 有覆盖 | 本人主题/回复及编辑页只读盘点未发现删除合同 | 不适用 | 不适用 | 不适用 |
| 附件选择与上传 | 明确不适用原生上传：表单可识别文件字段并在 POST/封存前拒绝；回复页两个同名 `Filedata` 仅降权登记。Android 原页可选已有文件，选择本身不上传 | 附件 POST 拒绝有覆盖；上传闭环未形成 | 发主题及回复/引用 GET 表单含文件字段；未执行上传 | 不适用 | 原页选择未验 | 未实现独立选择器；未验 |
| 投票 | 明确不适用：无已验证标准移动投票合同，主题页无原生或原页投票按钮；版块列表只展示投票状态徽章 | fail closed：`vote` 不在 `verifiedKinds` | 部分入口已盘点；未执行 POST | 不适用 | 不适用 | 不适用 |
| 点评写入 | 受控 WebView 兜底：Android/iOS 仅在登记移动 `comment` URI 且非缓存页显示「点评」并打开 `ForumOriginalPage`；Linux 隐藏该按钮。无原生点评写入 | 主题页 Linux 隐藏原页按钮、登记 URI 才显示有覆盖 | 入口已盘点；无标准 HTML form，未执行 POST | 不适用 | 未验 | 未验 |
| 评分写入 | 受控 WebView 兜底：Android/iOS 仅在登记移动 `rate` URI 且非缓存页显示「评分」并打开 `ForumOriginalPage`；Linux 隐藏。完整评分页原生解析关闭。无原生评分写入 | 评分 GET 无标准 form 时解析前 fail closed；主题页原页入口有覆盖 | 代表性响应无标准 `<form>`；未执行 POST | 不适用 | 未验 | 未验 |
| 举报 | 明确不适用：无已验证标准移动合同，主题页无原生或原页举报按钮 | fail closed：`report` 不在 `verifiedKinds` | 部分入口已盘点；未执行 POST | 不适用 | 不适用 | 不适用 |
| 私信发送和回复 | 原生标准 `pmform`、字段白名单、确认、单次 POST 和只读回读代码已接入 | 通过 | GET 表单契约已核对；未执行 POST | 未验 | 未验 | 未验 |
| 持久提交防重封存 | 数据库 schema v9 已加入按 uid/目标隔离的提交封存；论坛标准 Action、作品聚合收藏和私信发送路径均已接入 | 迁移、原子 attempted、并发 claim、进程恢复、结果未知、人工核对和成功事务有覆盖 | 不适用；未执行业务 POST | 未验 | 未验 | 未验 |

阶段 B 的原生写入仍只有七类已验证标准表单。删除、投票和举报已分类为明确不适用；附件上传明确不适用原生闭环；点评写入和评分写入已分类为 Android/iOS 受控 WebView 兜底。这不等于这些能力已在真实站或设备上验收。所有论坛业务 POST 均未在真实站或 Linux App 执行。`reply` / `quoteReply` / `editPost` 没有已验证成功码或成功跳转，任何非明确失败均保持 `resultUnknown`，回读成功也不能自动解除封存或删除草稿。版块收藏使用独立管线：添加 GET 之前原子写入 attempted 封存，取消仅在严格解析 GET 确认页后才封存并 POST；opaque 字段仅以 trim 判空并原样提交，取消表单 `referer` 只接受当前 uid 的只读版块收藏列表。两者均完整回读 `type=forum` 的全部分页后确认，未知结果保留封存，只能经回读确认或人工核对解除。`handlekey` 只作为 fresh 版块页提供的单一非空 opaque 值原样用于本次添加，不进入缓存、封存、错误文案或日志；现有证据不支持推断它与 fid/uid 的函数关系。

私信发送/回复已有标准表单代码和自动化，并在 POST 前原子落 `attempted` 封存、删除对应草稿；事务失败零 POST，进程重建后需要回读和人工解除。真实成功响应契约仍未经业务 POST 验证，因此不能自动重发，解除封存也只会重新 GET 表单。

## 受控 WebView 兜底

| 能力 | 当前实现 | 自动化 | 真实站 | Linux 实际 | Android | iOS |
| --- | --- | --- | --- | --- | --- | --- |
| 同源 HTTPS、登记路径和移动页导航限制 | 代码已接入 | 通过 | 未做 App 路径 | 不适用 | 未验 | 未验 |
| App/WebView Cookie 会话事务、身份切换/登出和崩溃恢复 | 代码已接入 | 自动化通过 | 未做 App 路径 | 不适用 | 未验 | 未验 |
| Cookie 属性安全桥 | 平台桥已接入；native→WebView 写入 `Secure`、`HttpOnly`、`Expires`、`Max-Age`、`SameSite`。iOS 完整回读；Android 受系统 `getCookie` 仅返回 `name=value` 限制，保守继承进入前属性，新认证 Cookie 默认 `Secure + HttpOnly + SameSite=Strict`，同名作用域歧义失败关闭；WAF Cookie 例外保持脚本可读且强制 `Secure` | Dart codec、属性合并、歧义失败关闭与会话恢复通过；Android Kotlin 离线编译通过 | 未验 | 不适用 | 未验 | 未验；Linux 无 Xcode，原生桥仅静态核对 |
| 原页附件文件选择 | Android 代码已接入受控系统文件选择器；仅打开已有文件，返回 `content://` 只读 URI，不负责上传 | Dart 适配器与失败关闭通过；Android debug 离线编译通过 | 未验 | 不适用 | 未验 | 未验 |

WebView 会话事务、Cookie 属性安全桥和 Android 文件选择器的自动化通过不代表兜底安全完成。Android 文件选择器只接受 `open` / `openMultiple`，拒绝保存模式，不调起相机、不自动上传；它通过 `ACTION_OPEN_DOCUMENT` 请求临时或持久只读授权，并在取消、并发、Activity 销毁或原页关闭时失败关闭。Android Cookie 属性回读不存在平台级完整属性 API，当前保守合并仍需实机验证服务端登录、登出、过期和身份切换；Android/iOS 实机生命周期、WAF、身份切换和附件路径全部关闭前，不得标记复杂原页兜底或移动端完成。Linux 不支持论坛原页，也不能替代这些门禁。

Linux 实时真实站验证默认只读。后续写验证只能在专用账号、明确开关、允许列表和专用目标同时满足时串行低频执行；Android/iOS 发布前仍须分别完成真实设备全路径。
